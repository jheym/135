# Define a lambda expression named f that accepts an integer parameter and
# converts it into the square of that integer; for example, if 4 were passed,
# your lambda would return 16. Do not write an entire function; just write a
# statement of the form:`f = lambda parameters: expression`

f = lambda a: a ** 2

print(f(6))